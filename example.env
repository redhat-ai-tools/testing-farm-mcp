# Testing Farm API Configuration
# Replace 'your_api_token_here' with your actual Testing Farm API token
TESTING_FARM_API_TOKEN=<testing-farm-api-key>

# Testing Farm API URLs (defaults are usually correct)
TESTING_FARM_API_URL=https://api.testing-farm.io/v0.1
TESTING_FARM_ARTIFACTS_URL=https://artifacts.dev.testing-farm.io

# MCP Transport Configuration
# Transport mode: 'stdio' for subprocess communication or 'sse' for web service
MCP_TRANSPORT=sse

# Port for SSE transport (only used when MCP_TRANSPORT=sse)
MCP_PORT=8000
MCP_HOST=127.0.0.1
